[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ak-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.27.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
ak-mcp = "ak_mcp:main"
